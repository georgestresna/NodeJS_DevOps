No dependencies momentarly, will only run app.js,
so i could have said '''COPY app.js . ''', but '''COPY . .'''
can be used along the way when scaling up the app.

Played around with the Dockerfile.
Prefferably run: sudo docker run --name appTest -p 3000:3000 appnodetest (give it a name); so that 
when i stop the app i can just run sudo docker stop appTest, and not need the ONTAINER ID

Github
git remote add gitlab git@.....             # add another git remote
git checkout master/gitlab/...              #switch branches
gir branch [-r(remote) -a(all)]optional     #view all branches
git push -u gitlab --all                    #copy the commit to gitlab from github

Gitlab YAML (https://spacelift.io/blog/gitlab-ci-yml#gitlabciyml-file-keywords)
image: ubuntu:latest #like the one in FROM in Dockerfile

stages: 
    - test
    - build
test_job:
    stage: test

cache:
    key: node_modules
    paths:
      - node_modules

#what to keep after the job is complete
build:
  artifacts:
    paths:
      - out/bin/

